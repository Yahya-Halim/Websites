# Optional Figma Handoff

Figma MCP / Code Connect are optional operator tools, not factory build requirements.

**Status for this run: no Figma source exists.** No Figma file, node, or design-token export was provided or produced for pita-kabab-2026-07-12-b5881b47. All design work lives in `design-system.md` (guidance) and `design-tokens.json` (build reference for Phase 4 `tokens.css`).

## Design Reference

- fileUrl: none — no Figma source for this run
- nodeId: none
- mode: read-only (n/a)

## Design Token Contract

- designTokenSource: not configured — `design-tokens.json` in this workbench is a hand-authored build reference, not a Figma/token-pipeline export
- designTokenExportPath: omitted from the sealed phase artifact (no handoff exists)
- designTokenHash: omitted from the sealed phase artifact (no handoff exists)
- Per the contract, the optional design-token handoff fields (designTokenSource, designTokenExportPath, designTokenHash) are omitted entirely from phase-3-artifact.json.

## Component Mapping References

None — no Figma components to map.

## Sync Status

not-configured

Do not paste access tokens, OAuth credentials, or private raw Figma file JSON into phase artifacts.
