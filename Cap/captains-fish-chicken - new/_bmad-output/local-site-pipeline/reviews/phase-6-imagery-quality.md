# Review — Phase 6 IMAGERY · reviewer: imagery-quality · verdict: PASS

The one required slot (logo) is resolved with a clean, legible vector wordmark that matches the
brand palette, and placing it cleared the asset-reference advisory in `factory validate`. Imagery
verify returns ok with the three dish slots correctly deferred as blocked-for-real-photo.

The important quality call: dish photos were NOT generated and passed off as the restaurant's real
food. For a real client, fabricated plates are a trust and accuracy risk, so blocking them awaiting
owner photos is exactly right. Hero is prompted and queued rather than faked. No fabricated imagery
shipped. PASS to launch.

Carried: generate the hero via Higgsfield when credentials are available; collect real dish photos
from the owner before promoting specific menu items with imagery.
